#include <stdio.h>

int main(){

    int a[5],sum=0,avg;
    printf("enter values=");
    for(int i=0;i<5;i++)
    {
        scanf("%d",&a[i]);
        sum+= a[i];
        avg = sum/5;
    }
    printf("average of numbers are=%d",avg);
     return 0;
}

#include <stdio.h>

int main(){

    int arr[5];
    printf("enter 5 numbers:");

    for(int i=0;i<5;i++)
    {
        scanf("%d",&arr[i]);

    }
    int max = arr[0];     //assume first element is largest
    for(int i=0;i<5;i++)
    {
        if(arr[i]>max)
        max = arr[i];
        }
    printf("largest no. is=%d",max);
        return 0;
}

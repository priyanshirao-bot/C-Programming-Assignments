#include <stdio.h>

int main(){

    int i=1,n,sum=0;
    printf("enter value of n");
    scanf("%d",&n);

    do
    {
        sum = sum+i;
        i++;
    } while (i<=n);
    
printf("sum of all integers from 1 to %d is %d",n,sum);
     return 0;
}

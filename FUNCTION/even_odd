#include<stdio.h>
int even(int n);
int even(int n){
    if(n%2 == 0){
        printf("even");
    }
    return even;
}

int odd(int n);
int odd(int n){
    if(n%2 !=0){
        printf("odd");
    }
     return odd;
}

int main(){
    int n;
    printf("enter value of n ");
    scanf("%d",&n);
    even(n);
    odd(n);
    return 0;
}

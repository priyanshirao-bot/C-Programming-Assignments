#include <stdio.h>

int main(){

    int arr[5],i,max;
   
    printf("enter 5 values of array:");
    for(i=0;i<5;i++){
        scanf("%d",&arr[i]);
        
    }
    max = arr[0];
    int *p = &max;
    for(int i=1;i<5;i++){
        if(arr[i]>max){
            max = arr[i];

        }
    }
        printf("largest element: %d\n",max);
        printf("largest element using pointer: %d\n",*p);
    
     return 0;
}
    

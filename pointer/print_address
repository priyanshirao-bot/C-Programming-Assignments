#include <stdio.h>

int main(){

    int a;
    printf("enter a value of a:");
    scanf("%d",&a);
    int *p = &a;
    printf("value of a: %d\n",a);
    printf("value of a using pointer: %d\n",*p);
     return 0;
}

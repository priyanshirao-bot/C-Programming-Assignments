#include <iostream>
using namespace std;
class rectangle{
    private:
    int l,b;
    public:
    void getdata(){
        cout<<"enter the length and breadth of rectangle";
        cin>>l>>b;

    }
    void area(){
        cout<<"area of rectangle is "<<l*b;

    }
};
int main(){
    rectangle r;
    r.getdata();
    r.area();
    return 0;
}
